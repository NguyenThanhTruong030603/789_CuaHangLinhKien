@model IEnumerable<CuaHangLinhKien.Models.Order>

@{
    ViewData["Title"] = "Index";
    decimal total = 0;
}

<h2>Orders</h2>

<table class="table">
    <thead>
        <tr>
            <th>Time</th>
            <th>TotalPrice</th>
            <th>ShippingAddress</th>
            <th>Notes</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>@product.OrderDate</td>
                <td>@product.TotalPrice</td>
                <td>@product.ShippingAddress</td>
                <td>@product.Notes</td>
                <td>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@product.Id">Delete</a>
                </td>
            </tr>
            total = total + product.TotalPrice;
        }
    </tbody>
</table>
<div class="card">
    <div class="card-body">
        <h5 class="btn btn-block btn-lg" style="width: 100%; cursor: default; background-color: #ffc107;">Tổng tiền là: @total</h5>
    </div>
</div>